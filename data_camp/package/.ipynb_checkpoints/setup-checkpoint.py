from setuptools import setup

setup(name = 'text_analyzer', 
      version = '0.0.1',
      description = 'practice of creating package', 
      author = 'Dana Kim', 
      author_email = 'ttcielott@gmail.com', 
      packages = ['text_analyzer'], 
      install_requires = ['pandas', 'numpy'])